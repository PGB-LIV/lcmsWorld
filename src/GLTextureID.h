#pragma once
#include "gl3w/gl3w.h"  

// a type that uniquely identifies a texture
// may vary for other implementations (i.e., anything non-openGL)
// but an int would typically be fine!

#define GLTextureID GLuint

